import {Main} from "../theme/Main";
import styled from "@emotion/styled";
import React from "react";
export const HeaderPlate =()=>{
    return(
        <div>
            <div>
                <div>Choose a game for boosting</div>
            </div>
        </div>
    );
}

